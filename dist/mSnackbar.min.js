(function ($) {
    jQuery.mSnackbar = function (text)
    {
        var start = function ()
        {
            if ($('mSnackbar').length )
            {

            }
            else
            {
                $('body').append('<div id="mSnackbar">'+text+'</div>');
            }

        }

        $('body').append('<div id="mSnackbarContainer"></div>');

        //var settings = $.extend({}, defaults, options);

        return {
            start: start
        }
    }

}(jQuery));
