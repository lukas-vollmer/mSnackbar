(function($) {

    $.fn.mSnackbar = function(text, options) {
        var settings = $.extend({
            displayTime  : 5000,
            color        : null,
            fontStyle    : null
        }, options);
    }

}(jQuery));